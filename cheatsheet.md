```yaml
# Atomic Knowledge Card Scaffold
- id: <идентификатор>      # уникальный ключ карточки
  Δεδομένα:                 # «данное мне» — исходные сырые данные (δεδομένα)
    заголовок: "<краткий заголовок>"
    описание: "<сырой текст боли или факта>"
    источник: "<автор или источник>"
    важность: "низкая|средняя|высокая"

  # Этап пайплайна:
  # capture   — фиксация «данной» точки (Echo)
  # basics    — структурирование (Weaver)
  # questions — проверка и формализация (Guard)
  # action    — применение решения (Loop + Jester)
  этап: <capture|basics|questions|action>

  # Действие на этапе:
  # seed      — создать capture-узел
  # structure — оформить поля для basics
  # check     — задать проверки для questions
  # patch     — применить AI-патч и пометить processed: true
  # merge     — финальное слияние
  действие: <seed|structure|check|patch|merge>

  структура:                 # результат обработки для basics и далее
    поле1: значение1
    поле2: значение2

  метаданные:
    создано: YYYY-MM-DD      # дата создания
    версия: 0.0.1            # версия карточки

  # Основной блокирующий камень:
  затруднение: "<описание ключевой проблемы>"

  # Фокус сегодняшней регенерации:
  фокус: "переосмысление кванторов ∀ и ∃"

  протокол:                  # как платформа и AI должны взаимодействовать
    команда: <seed|structure|check|patch|merge>
    параметры:
      количество: <N>         # для seed
      узел: <capture/seed-XX> # для других действий

  метки:                     # рубрики и категории
    - <метка1>
    - <метка2>

  заметки: "<свободные комментарии>"
```

